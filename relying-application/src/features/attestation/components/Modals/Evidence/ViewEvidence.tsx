import * as changeCase from 'change-case';
import { JsonEditor, githubDarkTheme } from 'json-edit-react';

import {
    ArtifactType,
    Infrastructure,
    Quote,
    Workloads,
} from '../../../types/attestation';
import { ModalHeader } from '../ModalHeader';

type Props = {
    artifactType: ArtifactType;
    evidence: Quote | Infrastructure | Workloads;
};

/**
 * Displays cryptographically signed evidence generated by the Attester.
 * Ensures traceability and verifiability of attestation results, fulfilling verifiability requirement V1.
 * Guarantees tamper-evident evidence, fulfilling trust minimization requirement TM2.
 */
export function ViewEvidence({ evidence, artifactType }: Props) {
    return (
        <div className="text-slate-100">
            <div className="flex flex-col max-h-[70vh] space-y-4">
                <ModalHeader title={changeCase.sentenceCase(artifactType)} />

                <div className="flex-1 overflow-y-auto rounded-md">
                    <JsonEditor
                        data={evidence}
                        viewOnly
                        maxWidth={'100%'}
                        theme={githubDarkTheme}
                    />
                </div>

                <div className="flex justify-end">
                    <button
                        className="flex h-8 cursor-pointer items-center gap-1.5 rounded-lg bg-teal-600/70 px-4 text-sm font-medium text-white transition-colors duration-200 hover:bg-teal-500"
                        onClick={() => {
                            console.log('Download'); // TODO: Add download logic.
                        }}
                    >
                        Download
                    </button>
                </div>
            </div>
        </div>
    );
}
